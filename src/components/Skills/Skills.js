import './Skills.css';
import SectionTitle from '../SectionTitle/SectionTitle';

export default function Skills() {
	return (
		<div className='skills contained'>
			<SectionTitle title="Technologies I've worked with" />
		</div>
	);
}
